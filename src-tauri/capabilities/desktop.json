{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities (shell, dialog for folder picker)",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": [
    "shell:allow-spawn",
    "shell:allow-execute",
    "dialog:allow-open"
  ]
}
